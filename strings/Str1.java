package corejavaprograms;

public class Test {
    public static void main(String[] args) {

        String[] arr = { "State Bank of India" };

        // Loop, but runs only once because array has one element
        for (int i = 0; i < arr.length; i++) {
            System.out.println("String = " + arr[i]);
        }
    }
}
